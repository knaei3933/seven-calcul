# パウチ見積システム 実装検証記録

## 実装スコープ
- Next.js App Router + TypeScript
- 日本語見積UI（入力・暫定結果・検証・顧客プレビューの3カラム/モバイル縦積み）
- `Decimal.js` による原価計算コア
- SKU別デジタルフィルム最小発注検証と構造化修正案
- API `/api/calculate`（失敗時は検証結果を返却）
- Playwrightブラウザ検証

## 実行方法
```bash
npm install
npm run dev
```

## 検証コマンド
```bash
npm run typecheck
npm test
npm run lint
npm run build
npm run e2e
```

## 確認済み計算
- マウスウォッシュ・2連・30ml/室・10,000枚・4列・ホッパ:
  - 充填対象 20,000室
  - テスト充填 60,000ml
  - バルク使用量 722,000ml
- 加圧充填は初期投入のみ 8,000ml に変化し、総バルク量は 728,000ml。
- カスタム費用は仕様あたり 400,000円 を1回のみ加算。
- 承認済み時のみ税抜販売額の20%を成功報酬として計算。
- 費用構成要素合計と最終原価の検算差額は 0。

## 未確定入力の扱い
- Seven化学書式
- 税区分・端数処理
- デジタル色数別価格
- カスタム寸法の列数・原反幅・価格帯変換

これらは既定値で発明せず、警告または確定ブロックとして扱う。
