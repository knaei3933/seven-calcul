# パウチ参考原価シミュレーター実装メモ

## SKUモデル（2026-09改訂）
- SKUは品目入力ではなく「同一条件の並列生産数」として扱う。
- 1生産セットの必要フィルム長さを内部Decimalで計算し、これをSKU数倍した総必要長さで発注検証する。
- 計算式: `数量 ÷ (1 - フィルムロス率) × (パウチ長さ + ピッチ加算mm) ÷ 1000 ÷ フィルム列数 × SKU数`。
- 各SKUの発注長さは自分の必要長さを100m単位で切り上げる。
- 供給最低値は画面パラメータで調整可能にする。既定は各SKU300m・合計500m。必要長が最小値未満の場合は自動補正し、`orderAdjustment`で理由を返す。
- SKU間は配列位置で独立計算し、SKU名・位置対応単価による混線は発生しない。

## 計算の前提
- 販売数量は連結後パウチ「枚」、充填対象はその枚数×連結室数「室」。
- 500回はテスト充填回数であり、1回の試験で`充填列数×充填量`を消費する。テスト充填はバルク使用量にのみ加算し、フィルム費には含めない。
- マウスウォッシュ45×145・2連・30ml/室・10,000枚・4列・ホッパ:
  - 充填対象20,000室、テスト充填60,000ml、バルク使用量662,000ml。
  - SKU数2の場合、1セット必要長419.444…m、総必要長838.888…m、各SKU発注500m、合計1,000m。
  - バルク単価の既定は0円/ml。0円でもDecimal計算・検算差額0を維持する。

## 画面・検証方針
- パラメータ画面からフィルムロス・最低発注長・配送・通関・バルクロス・試験回数・初期投入・加工固定費・フィルム単価を調整できる。
- 調整値は暫定計算・入力ハッシュ・サーバー計算に反映する。
- 結果パネルの「計算式を表示」で単位・置換済み数値・丸め規則を確認できる。
- 計算CTAは「計算する」であり、サーバー応答後の状態は「サーバー計算済み」（参照計算）とする。
- 色数別単価の参考入力状態と税・切上げ規則未確定は、発行・確定をブロックする。
- 画面金額は小数第2位四捨五入で表示し、合計・内部精算値はDecimal精度を保持する。

## 配送費モデル
- 配送回数は発注長さではなく必要生産換算長さ`数量 ÷ (1 - ロス率) × ピッチ ÷ 1000 ÷ 列数 × SKU数 × 生産倍率`を基準にする。
- `配送回数=ceil(必要生産換算長さ ÷ 配送単位m)`。
- 国内配送=回数×2,000円、海外配送=回数×16,000円。通関料はフィルム本体費>200,000円なら6,600円、以下なら回数×200円。

## 未確定・対象外
- Seven化学書式、税区分・端数処理、デジタル色数別公式単価、カスタム寸法の列数・原反幅・価格帯変換は未確定。
- フィルム単価の仕入先・見積番号・根拠PDF・有効期限・承認履歴は管理しない。
- 公式見積の保存・承認・発行、PDF出力、メール送信、変更履歴は対象外。
