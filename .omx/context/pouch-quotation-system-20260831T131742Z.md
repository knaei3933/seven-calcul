# Deep Interview Context: pouch-quotation-system

- Task: Make the existing pouch cost/quotation design more concrete for Seven Chemical delivery and Kanai Trading's 20% commission flow.
- Desired outcome: A homepage-based quotation calculator and Seven Chemical-aligned quotation output using cost calculation.
- User inputs requested: pouch width, fill volume, order quantity, filling method, connected-pouch pattern (single/double/triple/quadruple), custom-pouch flag, and printed color count.
- Business constraints supplied:
  - Kanai Trading issues the customer-facing quotation; after customer approval, this system owner receives 20% commission.
  - Digital fabric/film: total order must be at least 500m; each SKU must be at least 300m when total is >=500m.
  - Connected pouches: filling volume must be adjustable according to connected-pouch pattern.
  - Hopper filling (`ホッパ充填`): pieces × fill volume × 1.1 + initial charge + test quantity formula; user gave initial charge as 2L and test term as `500回 × 4列 × fill volume`.
  - Pressure filling (`加圧充填`): use initial charge 8L; remainder of bulk rule needs confirmation.
  - Custom pouch: apply a flat JPY 400,000 charge.
  - Printed color count affects digital film calculation versus gravure film; exact digital color pricing rule is not yet supplied.
- Existing design evidence:
  - Repository is documentation-only at inspection time; no README, AGENTS.md, docs directory, implementation code, package manifest, or tests were found.
  - `pouch-quotation-system-design.md` (649 lines, 21,246 bytes) already specifies Next.js/Tailwind/shadcn/Prisma or Supabase/NextAuth/Vercel architecture, Japanese JPY UI, cost model, digital film model, admin parameters, and phased implementation.
  - Existing open questions include admin auth, database choice, PDF format, language scope, gravure pricing, and custom-size policy.
- Intent hypothesis: replace manual Excel calculations with a customer-usable, constrained calculator that produces defensible approximate quantities and a deliverable Seven Chemical quotation while preserving commercial margins/commission.
- Unknowns: quotation payer/issuer/legal names/tax rules/document numbering; unit treatment for connected pouches; exact pressure-filling formula; digital color-count pricing; custom-size calculation; whether to modify design only or build an application now; non-goals and validation evidence.
- Decision-boundary unknowns: whether a reasonable first implementation may make documented defaults for quotation metadata, rounding, SKU splitting, and pricing bands, or must wait for Seven Chemical/Kanai Trading templates.
- Likely touchpoints: `pouch-quotation-system-design.md`; future calculator engine, UI, quotation PDF, and tests if implementation is requested.
- Terminology notes: user says digital fabric and film; existing document consistently uses digital printing/film. Japanese filling terms need canonical English/Korean/Japanese labels.
- Prompt-safe summary status: not_needed.
